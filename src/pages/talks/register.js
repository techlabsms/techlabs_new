import React, { useEffect } from "react"

const Register = () => {
  useEffect(() => {
    if (typeof window !== `undefined`)
      window.location.replace("http://eepurl.com/hjx355")
  })
  return <div></div>
}

export default Register
