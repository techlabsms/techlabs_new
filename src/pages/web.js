import React, { Component } from 'react';
import Navbar from '../components/Navbar';

class web extends Component {
    render() {
        return (
            <div>
            <Navbar/>
                web
            </div>
        );
    }
}

export default web;